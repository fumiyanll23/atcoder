#include <bits/stdc++.h>
using namespace std;

int main() {
  // input
  char c;
  cin >> c;

  // compute

  // output
  if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u') {
    cout << "vowel" << endl;
  }
  else {
    cout << "consonant" << endl;
  }
}
